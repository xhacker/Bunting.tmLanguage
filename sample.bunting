main {
    imm answer := 42 : float;
    mut string := "blue";

    print answer, $string, nl&;

    if (answer == 42 && 1 + 1 == 2) {
        mut A := create [int](0);
        imm charArray := ['x, 'h, 'a, 'c, 'k, 'e, 'r];
    }

    // this is a comment

    // short comment // imm num = 1.0e-623;
}
