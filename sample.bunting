main {
    imm afloat := 5 : float;
    imm achar := 'c;
    mut string := "blue";

    print afloat, achar, $string, nl&;

    if (1 + 1 == 3 || 2 + 2 == 4) {
        mut A := create [int](0);
        imm charArray := ['x, 'h, 'a, 'c, 'k, 'e, 'r];
    }

    // this is a comment

    // short comment // imm num = 1.0e-623;
}
